grpc:
  addr: ":9091"
http:
  addr: ":8082"
db:
  dsn: "pivot:pivot_pass_2026@tcp(localhost:3306)/pivot?charset=utf8mb4&parseTime=True&loc=Local"
log:
  level: "INFO"
jwt:
  access_ttl: 15m
  refresh_ttl: 168h
  issuer: pivot
  audience: user
  secret: "pivot-dev-jwt-secret-2026-change-in-prod"
minio:
  endpoint: "localhost:9000"
  bucket: "pivot"
  region: "us-east-1"
  access_key: "minioadmin"
  secret_key: "minioadmin"
